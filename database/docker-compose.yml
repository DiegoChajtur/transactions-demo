services:

  transactions_db:
    image: "postgres"
    environment:
      - POSTGRES_PASSWORD=dev_password
    volumes:
      - ./db.sql:/docker-entrypoint-initdb.d/db.sql
    ports:
      - "5432:5432"

  transactions_queue_db:
    image: "redis"
    ports:
      - "6379:6379"

